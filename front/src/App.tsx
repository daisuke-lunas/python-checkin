import { useState } from "react";

function App() {
  // 2-1: DBに記録されたチェックイン記録を閲覧し、何時にどう動いたかを見て料金計算するfrontUI

  /**
   * まずはチェックインしたユーザーIDの一覧を出す
   * クリックしたら、そのユーザーのチェックイン一覧を出す
   * min時とmax時を出し、比較し、時分単位で、お店の滞在時間を出す
   */
  return <>hello, world.</>;
}

export default App;
